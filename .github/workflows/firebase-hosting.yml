name: Firebase Hosting

on:
  pull_request:
    paths:
      - '.github/workflows/firebase-hosting.yml'
      - '.firebaserc'
      - 'firebase.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
      - 'apps/web/**'
      - 'packages/**'
  push:
    branches: [main]
    paths:
      - '.github/workflows/firebase-hosting.yml'
      - '.firebaserc'
      - 'firebase.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'turbo.json'
      - 'apps/web/**'
      - 'packages/**'
  workflow_dispatch:

concurrency:
  group: firebase-hosting-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Check Firebase secrets
        id: firebase
        shell: bash
        env:
          HAS_SA: ${{ secrets.FIREBASE_SERVICE_ACCOUNT != '' }}
          HAS_PID: ${{ secrets.FIREBASE_PROJECT_ID != '' }}
        run: |
          if [[ "$HAS_SA" == "true" && "$HAS_PID" == "true" ]]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Skipping deploy — set FIREBASE_SERVICE_ACCOUNT and FIREBASE_PROJECT_ID secrets to enable."
          fi

      - name: Checkout repository
        if: steps.firebase.outputs.enabled == 'true'
        uses: actions/checkout@v4

      - name: Setup pnpm
        if: steps.firebase.outputs.enabled == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 10.9.0

      - name: Setup Node.js
        if: steps.firebase.outputs.enabled == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        if: steps.firebase.outputs.enabled == 'true'
        run: pnpm install --frozen-lockfile

      - name: Validate workspace
        if: steps.firebase.outputs.enabled == 'true'
        run: |
          pnpm lint
          pnpm typecheck

      - name: Build web app
        if: steps.firebase.outputs.enabled == 'true'
        run: pnpm --filter @gravador/web build

      - name: Deploy to Firebase Hosting
        if: steps.firebase.outputs.enabled == 'true'
        env:
          FIREBASE_SA: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_PID: ${{ secrets.FIREBASE_PROJECT_ID }}
        run: |
          # Write service account key to temp file for auth
          printf '%s\n' "$FIREBASE_SA" > "$RUNNER_TEMP/sa-key.json"
          export GOOGLE_APPLICATION_CREDENTIALS="$RUNNER_TEMP/sa-key.json"

          # Deploy using Firebase CLI
          npx firebase-tools@latest deploy \
            --only hosting \
            --project "$FIREBASE_PID" \
            --non-interactive \
            --force

          # Cleanup
          rm -f "$RUNNER_TEMP/sa-key.json"
