# Trigger.dev worker running the @gravador/ai-pipeline tasks.
FROM node:22-alpine
RUN corepack enable && corepack prepare pnpm@10.9.0 --activate
WORKDIR /repo
COPY . .
RUN pnpm install --frozen-lockfile
WORKDIR /repo/workers/ai-pipeline
CMD ["pnpm", "dev"]
